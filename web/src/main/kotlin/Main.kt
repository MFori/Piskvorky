import kotlinx.browser.window

fun main() {
    Application().start()

    /*window.addEventListener("resize", {
        println("change+")

    })
    window.addEventListener("storage", {
        println("change")
    }, false)*/
}